/**
 * Constants for Group Management components
 */

/**
 * Debounce delay for search input (in milliseconds)
 */
export const SEARCH_DEBOUNCE_MS = 300

/**
 * Default number of batch groups to create
 */
export const DEFAULT_BATCH_GROUP_COUNT = 1

/**
 * Maximum number of batch groups that can be created at once
 */
export const MAX_BATCH_GROUP_COUNT = 20

/**
 * Duration for success/error messages (in milliseconds)
 */
export const MESSAGE_DURATION_MS = 3000
